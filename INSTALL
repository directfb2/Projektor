Projektor depends on LiTE, first compile and install it : 
https://github.com/directfb2/LiTE 

Projektor is built using Meson and Ninja:

  $ meson build/
  $ ninja -C build/

Finally, you can install Projektor using:

  $ ninja -C build/ install
